
spring:
   profiles:
      active: locale

   cloud:
      nacos:
         discovery:
            ip: null
            port: ${server.port}

      inetutils:
         ignored-interfaces:
            - docker0
            - veth.*
            - lo
            - virbr0
