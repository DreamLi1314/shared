server:
  port: 8761
eureka:
  instance:
    hostname: eureka-server # Eureka 实例的主机名

  client:
    register-with-eureka: false # 不把自己注册到 Eureka 上
    fetch-registry: false # 不从 Eureka 上获取注册信息.
    service-url:
      defaultZone: http://localhost:8761/eureka/ # 自定义注册中心的注册地址
